---
import Link from './Link.astro'

interface Props{
    title: string,
    href: string,
}

const {title, href } = Astro.props;

---
<Link href={href}}>
  <button tabindex="-1" class="block bg-accent/10 text-accent py-3 px-5 rounded-md pointer-events-none">
    {title}
  </button>
</Link>
