---
import Link from './Link.astro'

interface Props{
    title?: string,
    href: string,
}

const { title = "Button title", href } = Astro.props;

---
<Link href={href}>
    <button tabindex="-1" class="bg-accent-100 text-accent-400 py-2 px-5 rounded-md">
        {title}
    </button>
</Link>